# Install: `cargo install cargo-make`
# Help: https://sagiegurari.github.io/cargo-make/

extend = [{ path = "examples/Makefile.toml" }]

[env]
CARGO_MAKE_EXTEND_WORKSPACE_MAKEFILE = true

[config]
default_to_workspace = true
skip_core_tasks = true
skip_crate_env_info = true
skip_git_env_info = true
skip_rust_env_info = true

# -------------------------------------
# CI Checks
# -------------------------------------
[tasks.check]
dependencies = ["audit", "fmt", "lint", "outdated", "deps"]

[tasks.ci]
dependencies = [
  "fmt",
  "lint",
  "features",
  "hack",
  "tree-guard",
  "cli-guard",
  "test",
  "test-docs",
  "docs",
  "vet",
  "outdated",
  "deny",
]

# -------------------------------------
# Individual Actions
# -------------------------------------

# Audit
[tasks.audit]
command = "cargo"
args = ["audit"]

# Clean
[tasks.clean]
command = "cargo"
args = ["clean"]

# Deny
[tasks.deny]
command = "cargo"
args = ["deny", "--workspace", "check"]
install_crate = "cargo-deny"

# Deps
[tasks.deps]
script = '''
cargo +nightly udeps --all-targets
'''
install_crate = "cargo-udeps"
# args = ["machete", "--skip-target-dir"]
# install_crate = "cargo-machete"

# Feature gates: the flagged crates must stay warning-free without their
# default features so the gates don't rot (`lint` only covers --all-features).
[tasks.features]
script = '''
cargo clippy -p omnia -p omnia-wasi-sql -p omnia-wasi-identity -p omnia-guest --no-default-features --all-targets -- -D warnings
cargo clippy -p omnia --no-default-features --features cli --all-targets -- -D warnings
cargo clippy -p omnia --no-default-features --features jit --all-targets -- -D warnings
cargo clippy -p omnia --no-default-features --features otlp --all-targets -- -D warnings
cargo clippy -p omnia-cli --no-default-features --all-targets -- -D warnings
cargo clippy -p omnia-cli --no-default-features --features jit --all-targets -- -D warnings
for features in build guest host guest,host; do
  cargo clippy -p omnia-test --no-default-features --features "$features" --all-targets -- -D warnings
done
'''
install_crate = { rustup_component_name = "clippy" }

# Every feature combination of the published test crate must build on its
# own, without dev-dependencies masking a missing `dep:` line.
[tasks.hack]
command = "cargo"
args = [
  "hack",
  "check",
  "--feature-powerset",
  "--exclude-features",
  "default",
  "--no-dev-deps",
  "-p",
  "omnia-test",
]
install_crate = "cargo-hack"

# `omnia-test/build` must stay `std`-only so a consumer's `build.rs` never
# pulls the runtime into its build-dependency graph: the tree is one line.
[tasks.tree-guard]
script = '''
tree="$(cargo tree -p omnia-test --no-default-features --features build -e normal)"
if [ "$(printf '%s\n' "$tree" | wc -l)" -ne 1 ]; then
  printf 'omnia-test/build pulls dependencies:\n%s\n' "$tree" >&2
  exit 1
fi
'''

# A direct-command binary built without the facade's `cli` feature must link
# neither the grammar crate nor clap.
[tasks.cli-guard]
script = '''
tree="$(cargo tree -p omnia --no-default-features -e normal)"
if printf '%s\n' "$tree" | grep -Eq '(^|[^a-zA-Z0-9_-])(clap|omnia-cli) '; then
  printf 'omnia without `cli` pulls the CLI:\n%s\n' "$tree" >&2
  exit 1
fi
'''

# Semver check against the last published omnia-test; joins `ci` once the
# first release exists as a baseline.
[tasks.semver]
command = "cargo"
args = ["semver-checks", "-p", "omnia-test", "--all-features"]
install_crate = "cargo-semver-checks"

# Fmt (fix in place)
[tasks.fmt]
script = "cargo +nightly fmt --all"

# Lint
[tasks.lint]
command = "cargo"
args = ["clippy", "--workspace", "--all-targets", "--all-features", "--", "-D", "warnings"]
install_crate = { rustup_component_name = "clippy" }

# Miri
[tasks.miri]
script = '''
cargo +nightly miri setup
cargo +nightly miri nextest run --no-tests=pass
'''

[tasks.outdated]
script = '''
cargo outdated --workspace --exit-code 1 --depth 1 \
  --ignore cap-fs-ext --ignore cap-std --ignore cap-primitives \
  --ignore thiserror
'''
install_crate = "cargo-outdated"

[tasks.test]
command = "cargo"
args = ["nextest", "run", "--locked", "--all", "--all-features", "--no-tests=pass"]
env = { RUSTFLAGS = "-Dwarnings" }

[tasks.docs]
command = "cargo"
args = ["doc", "--no-deps", "--workspace", "--all-features", "--locked"]

[tasks.test-docs]
command = "cargo"
args = ["test", "--doc", "--all-features", "--workspace"]

# Vet
[tasks.vet]
script = '''
cargo vet regenerate imports
cargo vet regenerate exemptions
cargo vet regenerate unpublished
cargo vet --locked
'''
install_crate = "cargo-vet"

[tasks.publish]
command = "cargo"
args = ["publish", "--registry=augentic", "--allow-dirty", "--dry-run"]
