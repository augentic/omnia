# Model completion example.
#
# The `model` guest imports `omnia:model/completion` and calls `create`.
# The host serves it with an example-local backend (runtime.rs answers every
# completion with a fixed schema answer), so `create` returns a
# deterministic answer with no live model, no network, and no configuration.
#
# The `[[mount]]` preopens the repository root into every guest sandbox as a
# read-only workspace named `.`. The `model` guest reads it via
# `wasi:filesystem/preopens` and lends it through `grants.workspace`. The
# canned backend ignores the workspace (it never runs tools).
#
# Build the guest first (see README.md), then run (this path is compiled in
# via runtime! `manifest:`, so the binary is a direct command — no `run`
# subcommand):
#
#   cargo run --example model

# Named `model` here; without `name` the file's stem (`model_wasm`) would. A
# manifest file's guest loads at its first use, and only a guest loaded at
# boot can be command mode's catch-all, so the mark is what makes the run
# drive it.
[[guest]]
name = "model"
source.path = "../../target/wasm32-wasip2/debug/examples/model_wasm.wasm"
command = true

# Read-only workspace the `model` guest lends through `grants.workspace`.
# `path` resolves relative to this manifest's directory, so `../..` is the repo
# root; `name = "."` is what `preopens.get-directories()` returns and the guest
# selects on.
[[mount]]
name = "."
path = "../.."
